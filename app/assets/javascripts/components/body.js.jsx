var Body = React.createClass({
  // request: function(action, method, data){
  //   this.props.request(action, method, data);
  // },
  render: function() {
    return (
      <div>
        <p>Stuff in body</p>
        <QuestionList />
      </div>
    );
  }
});
